# DATABASE
POSTGRES_USER=app_user
POSTGRES_PASSWORD=ExAmPlEpAsSwOrD123!
POSTGRES_DB=affiliate_db

# APP SENSITIVE
SECRET_KEY=CHANGE_THIS_TO_A_VERY_SECURE_RANDOM_STRING
DOMAIN=app.domain.com

# FRONTEND
# Note: VITE_ variables must be present during BUILD time in Dockerfile if using standard build.
# For runtime config, a different approach is needed, but for simplicity we rely on build-arg or default.
VITE_API_URL=https://app.domain.com/api
